# Core backend — everything needed to run end-to-end with the TF-IDF fallback
# embedder and the deterministic stub assessor (no API key, no model download).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
scikit-learn==1.6.1
numpy==2.2.1
openpyxl==3.1.5
httpx==0.28.1
python-dotenv==1.0.1
